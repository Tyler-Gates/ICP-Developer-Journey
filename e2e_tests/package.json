{
  "devDependencies": {
    "isomorphic-fetch": "^3.0.0",
    "vitest": "^2.0.2"
  },
  "scripts": {
    "test": "vitest",
    "ci": "vitest run",
    "preci": "dfx stop; dfx start --clean --background; dfx deploy; dfx generate"
  },
  "dependencies": {
    "@dfinity/agent": "^1.4.0"
  }
}
